const {chromium}=require('playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('http://localhost/lcn/treatment_nd_test.php?id=5#ueberblick',{waitUntil:'domcontentloaded'});assert.equal(await p.locator('#ueberblick .overview-groups h3').first().innerText(),'Kurzbeschreibung');assert.equal(await p.locator('#ueberblick').getByRole('heading',{name:'Anwendung und Zugang',exact:true}).count(),0);assert.equal(await p.locator('#ueberblick').getByRole('heading',{name:'Anlaufstellen',exact:true}).count(),0);assert.equal(await p.locator('#ueberblick #alternativen').count(),1);assert.equal(await p.locator('#dashboard #alternativen').count(),0);assert.ok(await p.locator('#alternativen li').count()>0);assert.equal(await p.locator('#alternativen details').count(),0);await p.setViewportSize({width:390,height:844});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);console.log('PASS: overview description first, no access/contact teaser, visible relation lists in overview, mobile.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
