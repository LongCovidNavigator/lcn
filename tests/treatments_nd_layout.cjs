const {chromium}=require('playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1440,height:1000}});let errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost/lcn/treatment_nd_test.php?id=5#wirkung');await p.waitForSelector('[data-effect-percent]');assert.equal(await p.locator('#nd-demo-toggle').isChecked(),true);assert.deepEqual(await p.locator('.effect-label').allTextContents(),['Verschlechterung','Keine Veränderung','Verbesserung','Heilung']);assert.equal(await p.locator('[data-effect-percent="2"]').innerText(),'50 %');await p.locator('[data-effect-value="Verbesserung"]').click();assert.equal(await p.locator('[data-effect-value="Verbesserung"]').getAttribute('aria-pressed'),'true');await p.reload();assert.equal(await p.locator('[data-effect-value="Verbesserung"]').getAttribute('aria-pressed'),'true');await p.locator('#nd-demo-header-toggle').click();assert.equal(await p.locator('[data-effect-percent="2"]').innerText(),'–');await p.locator('#nd-demo-header-toggle').click();await p.screenshot({path:process.env.TEMP+'/lcn-effect-corrected.png',fullPage:true});await p.locator('.side-nav [data-view=ueberblick]').click();assert.ok(await p.getByRole('heading',{name:'Durchführung und zeitlicher Rahmen',exact:true}).isVisible());assert.ok(await p.getByRole('heading',{name:'Anwendung des Medikaments',exact:true}).isVisible());await p.locator('.side-nav [data-view=termin]').click();assert.equal(await p.locator('#termin').getByRole('heading',{name:'Anwendung des Medikaments',exact:true}).count(),0);assert.ok(await p.locator('[data-distribution-percent=pem]').first().isVisible());await p.locator('.side-nav [data-view=zugang]').click();assert.equal(await p.getByRole('heading',{name:'Konkrete Anlaufstellen',exact:true}).count(),0);await p.setViewportSize({width:390,height:844});await p.locator('.side-nav [data-view=wirkung]').click();assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);assert.deepEqual(errors,[]);console.log('PASS: default demo, four doctor bars, selection/reload, persistent header switch, moved overview blocks, removed access block, mobile.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
